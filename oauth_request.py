class OauthRequest:
	token = null
	secret = null
	
	def __init__(self, token, secret):
		self.token = token
		self.secret = secret
		
	#return list of user's tweets
	def getTweets:
		#request = get tweets api call
		#list = parsed json (tweet ids)
		
	def deleteTweet(tweets):
		#api call + tweet